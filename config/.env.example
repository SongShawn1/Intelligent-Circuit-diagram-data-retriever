# 电路图检索系统配置文件
# 复制此文件为 .env 并填入实际值

# ==================== 智谱 AI Embedding 配置（推荐） ====================
# 文档: https://docs.bigmodel.cn/cn/guide/models/embedding/embedding-3
#
# embedding-3 模型特点:
# - 价格: 0.5 元 / 百万 Tokens
# - 输入窗口: 8K tokens
# - 向量维度: 256-2048 可选（推荐 512）

# 智谱 AI API Key（必填）
ZHIPU_API_KEY=your-zhipu-api-key

# 智谱 AI API Base URL（可选，使用默认值）
# ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# ==================== 备用: OpenAI API 配置 ====================
# 如果需要使用 OpenAI 而不是智谱 AI，取消注释以下配置

# OPENAI_API_KEY=your_openai_api_key_here
# 可选：自定义 Base URL（用于 Azure OpenAI 或其他兼容服务）
# OPENAI_BASE_URL=https://api.openai.com/v1

# Embedding 模型选择
# 智谱: embedding-3 (推荐)
# OpenAI: text-embedding-3-small, text-embedding-3-large
EMBEDDING_MODEL=embedding-3

# 如果 API 不可用，自动回退到本地模型
# 本地模型可选:
#   - BAAI/bge-small-zh-v1.5 (推荐，512维，轻量)
#   - BAAI/bge-base-zh-v1.5 (768维，效果更好)
#   - shibing624/text2vec-base-chinese (768维，经典)
LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# ==================== ChromaDB 配置 ====================

# 向量数据库存储目录（相对于项目根目录）
CHROMA_PERSIST_DIR=chroma_db

# Collection 名称
CHROMA_COLLECTION_NAME=circuit_diagrams

# ==================== 检索配置 ====================

# 默认返回结果数量
DEFAULT_TOP_K=10

# 相似度阈值（低于此值的结果将被过滤）
SIMILARITY_THRESHOLD=0.3

# ==================== 应用配置 ====================

# 是否启用调试模式
DEBUG=false

# Streamlit 配置（部署时使用）
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=0.0.0.0
